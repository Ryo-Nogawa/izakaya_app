<h1>外観/内装</h1>

<% if current_user.admin? %>
  <%= link_to '追加', new_visual_path %>
<% end %>

<% @visuals.each do |visual| %>
  <%= link_to visual_path(visual.id) do %>
    <%= image_tag visual.image.variant(resize: '400x400') %>
  <% end %>
<% end %>