<%= search_form_for @p, url: search_drinks_path do |f| %>
  <%= f.collection_select :drink_category_id_eq, DrinkCategory.where.not(id: 1), :id, :name %>
  <%= button_tag type: 'submit',class: 'btn btn-outline-primary btn-sm ml-3' do %>
    <i class="fas fa-search"></i>
  <% end %>
<% end %>